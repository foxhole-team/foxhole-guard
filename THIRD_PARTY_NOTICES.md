# Third-party notices

FoxHole Guard is distributed under the GNU General Public License v3.0 or later
(see [LICENSE](LICENSE)). It ships and links third-party work whose licenses
require that their copyright and permission notices travel with the binary. This
file is that notice; it covers everything inside the APK.

For the exact versions of every Java/Kotlin dependency, see the CycloneDX SBOM
generated by `./gradlew cyclonedxBom -Pfoxhole.sbom=true` and attached to each
release. For the Rust side, see `sbom/*.cdx.json` in the FoxCore repository —
492 components on `aarch64-linux-android`, each with its license — and
`THIRD_PARTY_NOTICES.md` there.

---

## FoxCore — the network runtime

Built from source (`scripts/android-build.sh` in the FoxCore repository, pinned
by `config/foxcore-revision.txt`) and shipped as `libfoxhole_native.so`.

- License: GPL-3.0-or-later, © 2026 FOXHOLE TEAM
- Statically linked into that `.so` are ~492 Rust crates; their licenses are
  MIT, Apache-2.0, BSD-2/3-Clause, ISC, Unicode-3.0, MPL-2.0 and CC0-1.0. The
  full list with versions and license identifiers is the SBOM named above, which
  CI regenerates and `cargo deny check licenses` gates on both dependency graphs.

Notable components inside it:

### Arti — the Tor client

- Upstream: https://gitlab.torproject.org/tpo/core/arti
- License: **MIT OR Apache-2.0**, © The Tor Project, Inc. and contributors
- This is the Rust Tor implementation FoxCore links, **not** the C `tor` daemon.
  No standalone Tor binary is packaged.

---

## Tor pluggable transports (prebuilt)

Shipped as binaries under `app/src/main/assets/tor/<abi>/tor/pluggable_transports/`
and launched as managed transports for Arti's bridges. Taken from the Tor
Project's Android expert bundle
(`tor-expert-bundle-android-<abi>-15.0.9-foxhole-allbridges3`).

### lyrebird (obfs4proxy successor)

- Upstream: https://gitlab.torproject.org/tpo/anti-censorship/pluggable-transports/lyrebird
- License: **BSD-3-Clause**, © Yawning Angel and contributors
- Statically links ~50 Go modules (BSD-3-Clause, MIT, Apache-2.0, ISC). The
  Apache-2.0 ones (`aws-sdk-go-v2`, `smithy-go`, `golang/mock`, `tjfoc/gmsm`)
  additionally require their NOTICE text to be reproduced; run
  `go version -m lyrebird` on a shipped binary for the authoritative module list.

### conjure-client

- Upstream: https://github.com/refraction-networking/conjure
- License: **BSD-3-Clause**, © Refraction Networking
- Statically links ~34 Go modules under the same license families as above.
- Shipped for `arm64-v8a` and `armeabi-v7a` only; the x86 ABIs carry lyrebird
  but not conjure.

---

## i2pd — the I2P router

- Upstream: https://github.com/PurpleI2P/i2pd (pinned at `2.60.0`, see
  `third_party/i2pd.version`)
- License: **BSD-3-Clause**, © The PurpleI2P Project
- Vendored as source under `third_party/i2pd/` and built by
  `scripts/build-i2pd.sh` into `libi2pd.so`. The upstream license text ships
  with the source at `third_party/i2pd/LICENSE`.

---

## AdGuard DNS filter

- Upstream: https://github.com/AdguardTeam/AdGuardSDNSFilter
- License: **GPL-3.0**, © AdGuard Software Ltd
- Compiled into FoxCore's own FST format and shipped as
  `app/src/main/assets/rule-sets/adguard-dns-filter.fhds`. The source commit and
  the SHA-256 of the compiled artifact are recorded in
  `adguard-dns-filter.source.json`.

---

## Fonts

All three faces are licensed under the **SIL Open Font License, Version 1.1**;
the license text is vendored verbatim at `third_party/fonts/OFL.txt`, and the
per-face copyright lines below are taken from the shipped files' `name` tables.

- **LanaPixel** — © 2020 eishiya —
  https://opengameart.org/content/lanapixel-localization-friendly-pixel-font
- **Press Start 2P** — © 2012 The Press Start 2P Project Authors
  (cody@zone38.net), Reserved Font Name "Press Start 2P" —
  https://fonts.google.com/specimen/Press+Start+2P
- **Silkscreen Bold** — © 2001 The Silkscreen Project Authors —
  https://github.com/googlefonts/silkscreen

---

## Icons — 1-bit Pixel Icons

- Upstream: https://nikoichu.itch.io/pixel-icons (v1.2), author **Nikoichu**
- License: **CC0-1.0** (public domain dedication) — attribution is optional and
  given here anyway.
- Shipped as a ~39-icon subset, re-encoded white-on-transparent so the CLI theme
  can tint them.

---

## Country flags — R74n Pixel Flags

- Upstream: https://r74n.com/pixelflags/, author **R74n**
- License: **unresolved.** R74n publishes the pixel-art packs for free use, but
  the source page carries no explicit license text, so there is no grant we can
  reproduce here. 212 flags are shipped as
  `app/src/main/res/drawable-nodpi/flag_<iso2>.png`.
- Until an explicit grant is obtained from the author, or the set is replaced
  with one carrying a license (for example `flag-icons`, MIT), this component is
  redistributed on the strength of a permission that is not in writing. It is
  listed here rather than left implicit precisely because that is a decision
  someone has to make knowingly.

---

## Stalkerware indicators — SENTINEL threat intel

- Upstream: https://github.com/AssoEchap/stalkerware-indicators (`ioc.yaml`)
- License: **CC-BY-4.0** — © Echap (https://echap.eu.org) and contributors
- The bundled detector seed `app/src/main/assets/sentinel/threat-intel.json` is
  built from that file by `scripts/build-sentinel-threat-intel.py`; the upstream
  revision and its SHA-256 are recorded in `threat-intel.source.json` next to it.
- Only `type: stalkerware` entries are imported. Network indicators and YARA
  rules from the same repository are not: they need engines this app does not
  have, and their domains belong to the DNS pipeline instead.

---

## Map data — Natural Earth

- Upstream: https://www.naturalearthdata.com/
- License: **public domain** (Natural Earth terms of use: "no permission needed").
- The connection map's country outlines are generated from this data by
  `scripts/generate-traffic-map-shapes.py`.

---

## Android libraries

Resolved by Gradle and listed in full, with versions and license identifiers, in
the release SBOM. The ones whose licenses require an explicit notice:

- **lazysodium-android** — MPL-2.0 — https://github.com/terl/lazysodium-android
- **JNA** — LGPL-2.1-or-later OR Apache-2.0 — https://github.com/java-native-access/jna
- AndroidX / Jetpack Compose / Kotlin / kotlinx — Apache-2.0 — © The Android Open
  Source Project, JetBrains s.r.o. and contributors
- OkHttp, Okio — Apache-2.0 — © Square, Inc.
