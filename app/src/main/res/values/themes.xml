<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Foxhole" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">false</item>
    </style>

    <!-- Конфиг виджета: прозрачное окно, контент рисует собственную модальную карточку. -->
    <style name="Theme.Foxhole.WidgetConfig" parent="Theme.Foxhole">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <!-- Сплэш бренда: чёрный фон лого + лис по грудь; после — обычная тема. -->
    <style name="Theme.Foxhole.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#000000</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Foxhole</item>
    </style>
</resources>
